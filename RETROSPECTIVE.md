# 项目复盘：启思 AI 家教

> **一个不给答案的 AI 家教 MVP，从 PRD 到上线的完整过程**
>
> 作者：zhangxiaoteng86 · 2026 年 6 月

---

## 一、为什么做这个项目

起点是一个行业矛盾：市场上的 AI 家教硬件（学习机）动辄 2000–8000 元，而免费的拍照搜题 App 本质上是"帮孩子抄答案"，且面临双减合规风险。两条路都有明显问题。

我想验证一个假设：**能不能用纯软件的方式，做出一个不直接给答案、苏格拉底式引导的 K12 学习产品？**

这就是「启思 AI 家教」的出发点。我自己写了一份完整的 PRD（产品需求文档），然后从零把它实现出来。

---

## 二、先写 PRD，再写代码

很多个人项目会跳过产品设计阶段，直接开始敲键盘。我有意反过来——**先把 PRD 写到足够清晰，再开始动代码**。

这让我提前想清楚了几个关键问题：

**1. 核心机制是什么？**
苏格拉底引导不只是"聊天"，它需要一个**状态机**：当前解题到第几步（`stepIndex`）、学生已经看了几档提示（`hintLevel`）、连续错了几次（`wrongStreak`）——每一轮 AI 的回复，都由这个状态决定，不能随机。

**2. 最高优先级的质量风险是什么？**
PRD 把"提前泄题"列为最高优先级 Badcase：AI 在学生还没求助到第④档时，就把答案给出来了。这个风险在脚本引擎里可以完全控制，但接入真实大模型后，模型有可能"好心"给出答案。所以我在架构上从一开始就设计了**后置校验层**——模型的每条回复在下发前都要过一遍检查。

**3. 数据怎么算？**
PRD 要求掌握度三档、错题本、家长周报、北极星指标（周独立做题率）。我的结论是：**不能硬编码任何数字，所有结论必须从埋点事件聚合**。这个决策保证了数据是真实可信的，也让"越用越准"真的能体现出来。

---

## 三、技术架构：几个关键决策

### 决策一：纯前端，零构建

我选择了不引入任何框架或构建工具——只有 HTML + CSS + 原生 JavaScript，双击 `index.html` 就能跑。

**原因：**
- 这是一个 Demo，核心价值在逻辑，不在框架选型；
- 零依赖意味着可以直接部署到 GitHub Pages，无需任何服务器；
- 也倒逼我把架构分层做清楚：`data.js → kb.js → store.js → engine.js → app.js`，每一层职责单一。

代价是没有热更新、没有类型检查。但对这个体量的项目，这是划算的。

### 决策二：双内核 + 可插拔接口

引擎分两种工作模式：

```
默认：规则脚本引擎（离线 · 稳定 · 防泄题 100% 可控）
      ↕ 一键切换
AI 模式：BYOK 直连 Claude（能引导任意题，受后置校验约束）
```

这个决策的核心思路是**把"接地"和"生成"分开**：
- 脚本引擎保证核心产品逻辑可演示、可验证；
- AI 模式保证有真实模型的可扩展性；
- 两者共用同一套状态机和后置校验，切换时引擎行为一致。

BYOK（用户自带 Key）而非"帮用户付费"，是故意的设计选择——公开 Demo 不适合自己承担模型费用，且把 Key 存在用户浏览器本地本身就是一个值得记录的工程决策。

### 决策三：事件驱动的数据层

```
所有用户行为 → track(event) → localStorage 事件流
                                    ↓
           掌握度 / 错题本 / 遗忘曲线 / 周报 / 首页指标
```

设计原则来自 PRD 第 9 章的埋点需求：**事实只有一份（事件流），所有派生数据都从中聚合**。好处是：
- 任何"指标算法"的变化，只需改聚合函数，不需要迁移历史数据；
- 可以在界面里直接展示事件流，把数据闭环变成可演示的特性；
- 遗忘曲线调度也直接从 `guide_solved` 事件自动触发，引擎完全无需感知。

### 决策四：RAG 接地而不是 RAG 生成

一个常见误区是把 RAG 用来"让模型回答任意问题"。我的使用场景更聚焦：**用检索保证模型回复的数学知识正确、与课标对齐**（PRD 7.1 的硬性要求）。

具体做法：拍题确认时检索知识库，检索结果在 UI 里可视化展示（即使在免费脚本模式里也看得见），AI 模式下把检索结果注入 kickoff 提示词。检索器目前是稀疏打分（知识点匹配 + 关键词重叠），接口设计成可平滑升级到向量嵌入。

---

## 四、遇到的真实问题

### 问题一：答案判定比想象的难

最初以为用字符串相等就够了。实际上学生的回答形式五花八门：

```
正确答案: "4"
学生可能输入: "x=4" / "宽=4米" / "4 米" / "X＝4"（全角）/ "x²+2x-24=0"（平方用²）
```

我写了一套归一化函数：去空格、全角转半角、`x²→x^2`、剥离"x=" 前缀、剥离单位、数值近似比较。最后自测：所有 14 道题按标准答案的第一个变体走一遍，都能独立做出（`hintLevel = 0`）。这变成了测试套件里的一个永久回归用例。

### 问题二：异步与同步混合的调度问题

脚本引擎的 `start()` / `submit()` 是同步的；LLM 模式是异步的（返回 Promise）。UI 层不应该关心引擎是同步还是异步。

解法：在 UI 的 `dispatch()` 函数里统一用 `Promise.resolve()` 包裹，同步结果会被立即 resolve，异步结果正常等待。一行代码解决了两种引擎的统一调度：

```javascript
function dispatch(res) {
  Promise.resolve(res).then(out => renderInput(out)).catch(err => showError(err));
}
```

### 问题三：测试用例的竞态

测试套件里有一条异步的防泄题测试，它会临时把全局 `QisiLLM.enabled` 改成 `true`——如果这条测试和后面的同步测试并发，后面的测试会在"LLM 模式"下跑，得到错误结论。

排查后发现根本原因：`test()` 函数不等异步用例 resolve，就继续注册下一条。修法：把防泄题测试放到所有测试的**最后**注册，并把断言改成依赖 session 的本地计数而非全局事件，彻底消除竞态。这是写测试时很容易踩的坑。

---

## 五、验证

写了一个可以在浏览器直接打开的测试套件（`tests/test.html`），22 条断言覆盖核心逻辑。
**[线上可点开验证 →](https://zhangxiaoteng86.github.io/qisi-ai-tutor/tests/test.html)**

关键用例：

| 类别 | 用例 | 验证方式 |
|---|---|---|
| 等价判定 | `x=4` / `4 米` / `X＝4` 都判对；`6` 判错 | 断言 |
| 可解性 | 14 道题按标准答案全部「独立做出」 | 批量断言 |
| 提示阶梯 | 每次只升一档，第④档触发终局 | 状态检查 |
| 防泄题 | Mock 模型返回含答案的文本 → 被拦截降级 | 异步断言 |
| 遗忘曲线 | 独立做对 → 排到将来；失败 → 当天到期 | 时间偏移测试 |

---

## 六、我学到的

**1. PRD 迫使你提前面对最难的设计问题。**
写 PRD 的过程让我在敲代码之前就想清楚了"状态机的字段是什么"、"掌握度怎么算"、"防泄题的校验位置在哪"。事后来看，这省了大量返工。

**2. 可演示的约束比文字描述有说服力。**
"防泄题"说起来是一句话，但让它在 Demo 里可观测（开 AI 模式、在埋点面板看到 `leak_blocked` 事件）完全不同——前者是 PRD 里的一个条目，后者是一个真实运作的安全层。

**3. 测试是架构清晰度的照妖镜。**
写测试时遇到的竞态问题，根源是全局状态管理不够干净。它逼我把防泄题测试改成依赖 session 本地计数而非全局事件——这个修法同时改善了生产代码的可测试性。

**4. BYOK 是面向 Demo 的正确选择。**
最初我考虑过做后端代理让用户不用填 Key，但仔细算了一下：公开 Demo 会被陌生人刷接口，API 费用完全不可控。BYOK 让用户自己承担模型费用，同时在 README 里说明"生产环境应走后端代理"——这个说明本身就展示了我对安全架构的理解，反而是加分项。

---

## 七、如果给项目一个 V2

如果继续迭代，我会优先做：

1. **RAG 升级到向量嵌入**：现在的稀疏检索在关键词不重叠时会漏检；用课标知识库的嵌入向量做语义检索，准确率会更高，也是更"标准"的 RAG 实现。

2. **真实用户数据**：现在所有数据都是本地的，没有服务端。如果接入轻量的后端（哪怕只是一个 Supabase 免费实例），就能看到"真实用户的独立做题率是否真的比搜题 App 高"——这才是 PRD 第 2.2 节想验证的核心假设。

3. **更多知识点的题库**：现在 14 道题能把数据闭环跑起来，但要验证"遗忘曲线推荐"的实际效果，每个知识点至少需要 5 道以上的变式题。

---

**在线 Demo**：[https://zhangxiaoteng86.github.io/qisi-ai-tutor/](https://zhangxiaoteng86.github.io/qisi-ai-tutor/)
**代码仓库**：[https://github.com/zhangxiaoteng86/qisi-ai-tutor](https://github.com/zhangxiaoteng86/qisi-ai-tutor)
